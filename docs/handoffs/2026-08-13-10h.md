# Handoff — 2026-08-13 10h

## Estado em voo

Auditoria completa de `skills/` por neutralidade/redundância/discrepância: 54 arquivos
corrigidos em 5 commits (pushed). Depois, `skills/mcp/notebooklm-harness.md` foi
reconciliada com o pacote MCP real `@charlie.act7/gemini-notebook-mcp` (a versão antiga
citava um `nlm login` e ferramentas que não existem) — commitado e pushed.

MCP `gemini-notebook` conectado agora, escopo `user` (`~/.claude.json`, disponível em
todo projeto desta máquina), conta Google default (sem `NOTEBOOKLM_ACCOUNT`).
`claude mcp get gemini-notebook` confirma `Connected`, mas as tools só aparecem numa
sessão nova (MCP carrega no start, não a quente). Rastreado na issue #255.

## Estado do router (para retomada)

- tier_final: sonnet
- fase_final: implement
- debug_travado: 0
- arquivos_tocados: 55 (54 do audit + notebooklm-harness.md)
- risco_corrente: baixo

## Issues abertas relevantes

- **#255** (nova) — concluir setup do NotebookLM: `setup_auth` → criar notebook manual
  no site → `add_notebook`/`import_account_notebook` + `select_notebook` → curadoria
  (Passo 3 da skill) → doc de governança (Passo 4) → registrar no CLAUDE.md (Passo 5).
- #36 — adoção de `/abrirsessao`/`/fecharsessao` como ciclo padrão segue aberta; esta
  sessão usou o protocolo pontualmente mas não fechou essa decisão de adoção formal.
- Achado à parte, não urgente: este repo tem duas camadas de continuidade de sessão que
  não se falam — o plugin `remember` (injeta automaticamente no SessionStart, mas grava
  só localmente em `.remember/`, gitignored) e este handoff (`docs/handoffs/`, commitado
  no Git, é o que `/abrirsessao` de fato lê). Nenhum doc do acervo declara precedência
  entre os dois, ao contrário do que existe para `superpowers` em
  `docs/precedencia-de-skills.md`. Vale um documento equivalente no futuro.

## Guard-rails ativos

- Não usar `label:task`/`label:scheduled` em `gh issue create` — esses labels não
  existem neste repo (só `bug`, `enhancement`, `documentation`, etc. — ver `gh label
  list`). As skills de workflow assumem labels que este repo nunca criou.
- `docs/debt-ledger.md` não existe — `skills/audit/repo-overengineering-audit.md` está
  pendente de rodar por este critério, mas não foi executado nesta sessão (fora de
  escopo do que foi pedido).

## Próxima ação recomendada

Nova sessão: rodar `setup_auth` (MCP `gemini-notebook`) com a conta Google default,
confirmar `get_health`, e seguir o Passo 2 de `skills/mcp/notebooklm-harness.md`.

## Decisões desta sessão

- Ao fundir skills redundantes, decisão de manter `.claude/skills/eruda/SKILL.md` e
  `skills/devops/eruda-mobile-debug.md` como duas skills (histórico documentado no
  CHANGELOG), só cross-referenciadas — não fundidas.
- MCP `gemini-notebook` registrado em escopo `user` (global), não `.mcp.json` do
  projeto — decisão explícita do usuário.
