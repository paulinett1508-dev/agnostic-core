# Handoff — 2026-07-14 22h08

## Estado em voo

Sessão concluída e 100% shipada — nada pela metade. Entregou o **agnostic-router**
(roteamento comportamental de modelo, v1.2.0, release latest no GitHub), **reconciliou** as
duas skills de roteamento de modelo e limpou o drift de model_id no acervo. Tudo commitado
e pushed em `origin/master`; working tree sem mudanças pendentes reais (só a camada
`.claude/skills/` gerada, não-versionada por norma, + `.claude/settings.local.json` local).

Trilha desta sessão (todos os commits em master):

- `feat(ai): agnostic-router` (PR #184, squash `eaa9226`) — skill `skills/ai/agnostic-router.md`,
  motor `scripts/agnostic-router/{router.py,eval.py}`, design `docs/agnostic-router-design.md`,
  roteamento mesclado em `abrirsessao`/`fecharsessao`, índices atualizados.
- `fix(scripts)` `729b227` — gerador ignora `#` dentro de code fence.
- `docs(claude)` `34b8716` — protocolo de roteamento no CLAUDE.md.
- `chore(lint)` `da756d6` — `.claude/skills` excluído do markdownlint.
- `docs(changelog)` `e327cd8` — CHANGELOG + bump 1.1.1→1.2.0. Release `v1.2.0` (latest).
- `refactor(skills)` `05ff701` — reconcilia model-routing (política) × agnostic-router (mecanismo).
- `fix(skills)` `192c30c` — model_id `claude-sonnet-4-6` fantasma → `claude-sonnet-5` (3 skills).

## Estado do router (para retomada)

- tier_final: barato (haiku)
- fase_final: operate (fechamento — commits, release, handoff)
- debug_travado: 0
- arquivos_tocados: ~14
- risco_corrente: baixo

## Issues abertas relevantes

- **#181** [convenção] Roteamento de modelo em subagentes — relacionada. Referencia
  `behavioral-model-routing`, que agora está reconciliada com o `agnostic-router`
  (política × mecanismo). Comentário de cross-referência já postado nesta sessão. Segue
  ABERTA — o "done" dela é prática operacional de dispatch, comportamental e independe do engine.
- #36 adotar `/abrirsessao` e `/fecharsessao` como ciclo padrão.
- #166 master e main divergiram estruturalmente.

## Guard-rails ativos

- **Camada `.claude/skills/` é gerada e não-versionada** (só `eruda` é rastreada, à mão).
  Regenerar com `bash scripts/generate-claude-skills.sh`; nunca commitar as 111 geradas.
  O lint já as ignora.
- **Este repo É o próprio agnostic-core** — nunca adicionar `.agnostic-core` como submódulo
  (auto-referência recursiva). Promoção de padrões é direto na árvore.
- **Duas skills de roteamento agora reconciliadas** (não é mais dilema): `behavioral-model-routing`
  = POLÍTICA, `ai-agnostic-router` = MECANISMO, com cross-ref bidirecional. Ao editar uma,
  checar a outra — a política é a fonte da regra, o engine a executa.
- Python não está no PATH; usar `C:\Python312\python.exe` (console cp1252 → scripts forçam UTF-8).

## Próxima ação recomendada

Nada pendente/bloqueante. Loop da #181 fechado (comentário de cross-referência postado).

## Decisões desta sessão

- Motor Python em `scripts/agnostic-router/` (não em `skills/`, que é markdown-puro/agnóstico);
  a skill markdown é a fonte da verdade, o Python é exemplo de referência.
- Comandos de sessão MESCLADOS (não duplicados); `newsession` do pacote original NÃO
  reintroduzido (repo já o deprecou em favor de `/abrirsessao`).
- Reconciliação política×mecanismo em vez de fundir as duas skills — preserva a distinção
  behavioral (auto-invoke) vs técnica (confirma) e a regra de elegância (não bloatar).
- Fix de model_id repo-wide: `claude-sonnet-4-6` (inexistente) → `claude-sonnet-5`;
  `claude-opus-4-6` → `claude-opus-4-8`.
- v1.2.0 como release datada (repo não usa Unreleased); package.json bumpado junto.
