# Handoff — 2026-07-14 21h51

## Estado em voo

Sessão concluída e 100% shipada — nada pela metade. Entregou o **agnostic-router**
(roteamento comportamental de modelo) no acervo, versão **v1.2.0** publicada como
latest release no GitHub. Tudo commitado e pushed em `origin/master`; working tree
sem mudanças pendentes reais (só a camada `.claude/skills/` gerada, não-versionada por
norma, + `.claude/settings.local.json` local).

Trilha desta sessão (todos os commits em master):

- `feat(ai): agnostic-router` (PR #184, squash `eaa9226`) — skill `skills/ai/agnostic-router.md`,
  motor `scripts/agnostic-router/{router.py,eval.py}`, design `docs/agnostic-router-design.md`,
  roteamento mesclado em `abrirsessao`/`fecharsessao`, índices atualizados.
- `fix(scripts)` `729b227` — gerador ignora `#` dentro de code fence (bug de descrição).
- `docs(claude)` `34b8716` — protocolo de roteamento no CLAUDE.md.
- `chore(lint)` `da756d6` — `.claude/skills` excluído do markdownlint.
- `docs(changelog)` `e327cd8` — CHANGELOG + bump 1.1.1→1.2.0.
- Release `v1.2.0` criada e marcada como latest.

## Estado do router (para retomada)

- tier_final: barato (haiku)
- fase_final: operate (tarefas mecânicas de fechamento — commit, release, changelog)
- debug_travado: 0
- arquivos_tocados: ~10
- risco_corrente: baixo

## Issues abertas relevantes

- **#181** [convenção] Roteamento de modelo em subagentes — RELACIONADA mas NÃO resolvida
  por esta sessão. É sobre escolher tier ao despachar subagentes (referencia a skill
  `behavioral-model-routing`); o `agnostic-router` é infraestrutura complementar
  (engine + protocolo declarado), não a mesma coisa. Ver guard-rail abaixo.
- #36 adotar `/abrirsessao` e `/fecharsessao` como ciclo padrão.
- #166 master e main divergiram estruturalmente.

## Guard-rails ativos

- **Duas skills de roteamento de modelo coexistem agora**: `behavioral-model-routing`
  (convenção de tier em dispatch de subagente, ligada à #181) e `ai-agnostic-router`
  (engine determinístico + protocolo declarado). NÃO mesclar às cegas — decidir
  conscientemente se reconciliar, cross-referenciar ou manter separadas por propósito.
- **Camada `.claude/skills/` é gerada e não-versionada** (só `eruda` é rastreada, à mão).
  Regenerar com `bash scripts/generate-claude-skills.sh`; nunca commitar as 111 geradas.
  O lint já as ignora.
- **Este repo É o próprio agnostic-core** — nunca adicionar `.agnostic-core` como submódulo
  (auto-referência recursiva). Promoção de padrões é direto na árvore.
- Python não está no PATH; usar `C:\Python312\python.exe` (console cp1252 → scripts forçam UTF-8).

## Próxima ação recomendada

Decidir o destino da sobreposição `behavioral-model-routing` × `ai-agnostic-router`:
cross-referenciar as duas skills (uma aponta pra outra) OU consolidar. Se consolidar,
atualizar #181 e `docs/keywords-map.md`. Não é urgente — ambas funcionam isoladas.

## Decisões desta sessão

- Motor Python vai em `scripts/agnostic-router/` (não em `skills/`, que é markdown-puro/agnóstico);
  a skill markdown é a fonte da verdade e o Python é exemplo de referência.
- Comandos de sessão foram MESCLADOS (não duplicados); `newsession` do pacote original
  NÃO foi reintroduzido pois o repo já o deprecou em favor de `/abrirsessao`.
- Fix do model_id: `claude-sonnet-4-6` (inexistente) → `claude-sonnet-5`.
- v1.2.0 tratada como release datada (repo não usa seção Unreleased); package.json
  bumpado junto pra manter o invariante changelog↔versão.
