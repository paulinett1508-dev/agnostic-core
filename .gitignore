# Claude Code runtime (gerados por sessão, nao commitar)
.claude/scheduled_tasks.lock
.claude/**/*.lock
.claude/output-mode

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Node / tooling (caso algum script gere)
node_modules/
*.log

# Python (artefatos de rodar os scripts, ex.: agnostic-router)
__pycache__/
*.pyc

# Scripts internos do autor — não distribuídos
# Coloque aqui utilitários pessoais (update em massa, patches, etc.)
scripts/internal/

# Notas internas do autor (contexto pessoal, lista de projetos, etc)
CLAUDE.internal.md
