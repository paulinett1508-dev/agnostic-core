name: Check Refs

on:
  push:
    branches: [main]
  pull_request:

jobs:
  refs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate .agnostic-core refs + SKILL.md frontmatter
        run: bash scripts/check-refs.sh
